# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/gms
spring.datasource.username=root
spring.datasource.password=Amanjeet@4321.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
# Use Railway's dynamic PORT if provided, otherwise default to 8080 for local dev
server.port=${PORT:8080}

# CORS Configuration (handled in controllers with @CrossOrigin)

